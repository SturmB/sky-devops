---
# Database vars
db_acsays_database: sky
db_schedule_database: sky_schedule
db_username: skyweb
db_password: sky241

# Upgrade vars
upgrade_aa: no
upgrade_acs: no
upgrade_ays: no
upgrade_schedule: no

# Miscellaneous vars
recreate_containers: yes

# geerlingguy.pip
pip_install_packages:
  - name: docker

# geerlingguy.docker
docker_users:
  - "{{ ansible_user }}"
